interface:
  display_name: "TDD Workflow"
  short_description: "Run a work unit with TDD"
  default_prompt: "Read the work specification and complete test-list items through TDD with one commit per item."
