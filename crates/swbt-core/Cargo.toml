[package]
name = "swbt-core"
version = "0.1.0"
edition = "2024"
rust-version = "1.87"
description = "Backend-independent model, input, profile, and protocol core for swbt."
repository = "https://github.com/niart120/swbt-rs"
license = "MIT"
readme = "README.md"
publish = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
